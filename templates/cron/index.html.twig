{% extends 'base.html.twig' %}

{% block title %}Configuration Cron{% endblock %}

{% block body %}

    {% for label, messages in app.flashes %}
        {% for message in messages %}
            <div class="alert alert-{{ label }}">
                {{ message }}
            </div>
        {% endfor %}
    {% endfor %}
    <div class="bg-white shadow-md rounded-lg p-6 mt-4">
        <div class="card">
            <div class="card-header mb-4">
                <h3 class="text-lg font-medium">Choisir une fréquence</h3>
            </div>
            <div class="card-body">
                {{ form_start(form) }}
                <div class="mb-4">
                    {{ form_widget(form) }}
                </div>
                <button type="submit" class="btn btn-primary btn-sm">
                    Enregistrer le Cron
                </button>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
    </div>

{% endblock %}
